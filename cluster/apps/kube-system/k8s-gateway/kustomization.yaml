---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
  - rbac.yaml
configMapGenerator:
  - name: k8s-gateway
    files:
      - ./Corefile
generatorOptions:
  disableNameSuffixHash: true
