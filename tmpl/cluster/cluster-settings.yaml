---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
data:
  TIMEZONE: ${BOOTSTRAP_TIMEZONE}
  METALLB_LB_RANGE: ${BOOTSTRAP_METALLB_LB_RANGE}
  METALLB_K8S_GATEWAY_ADDR: ${BOOTSTRAP_METALLB_K8S_GATEWAY_ADDR}
  METALLB_TRAEFIK_ADDR: ${BOOTSTRAP_METALLB_TRAEFIK_ADDR}
  METALLB_PLEX_ADDR: ${BOOTSTRAP_METALLB_PLEX_ADDR}
  METALLB_HOME_ASSISTANT_ADDR: ${BOOTSTRAP_METALLB_HOME_ASSISTANT_ADDR}
  METALLB_MINIO_ADDR: ${BOOTSTRAP_METALLB_MINIO_ADDR}
  METALLB_VECTOR_ADDR: ${BOOTSTRAP_METALLB_VECTOR_ADDR}
  CLUSTER_NAME: ${BOOTSTRAP_CLUSTER_NAME}
  CLUSTER_HOST_MASTER_ADDR_1: ${BOOTSTRAP_ANSIBLE_HOST_ADDR_0}
  CLUSTER_HOST_MASTER_ADDR_2: ${BOOTSTRAP_ANSIBLE_HOST_ADDR_1}
  CLUSTER_HOST_MASTER_ADDR_3: ${BOOTSTRAP_ANSIBLE_HOST_ADDR_2}
