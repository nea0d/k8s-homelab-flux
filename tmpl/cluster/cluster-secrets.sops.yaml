# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
  SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
  SECRET_LONGHORN_BACKUP_TARGET: ${BOOTSTRAP_LONGHORN_BACKUP_TARGET}
  SECRET_LONGHORN_STORAGE_MOUNT_PATH: ${BOOTSTRAP_LONGHORN_STORAGE_MOUNT_PATH}
  SECRET_ALERT_MANAGER_MATTERMOST_WEBHOOK: ${BOOTSTRAP_ALERT_MANAGER_MATTERMOST_WEBHOOK}
  SECRET_PLEX_CLAIM: ${BOOTSTRAP_PLEX_CLAIM}
