# yamllint disable
apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
  name: postgres-superuser
  namespace: databases
stringData:
  username: ${BOOTSTRAP_POSTGRES_SUPER_USERNAME}
  password: ${BOOTSTRAP_POSTGRES_SUPER_PASSWORD}
